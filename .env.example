# AI Poker Night — Root Environment Reference
# ================================================
# Server environment variables are configured in server/.env
# Copy server/.env.example → server/.env and fill in your values.

# Quick setup:
#   cp server/.env.example server/.env
#   # Edit server/.env with your private key and deployed contract addresses

# See server/.env.example for the full list of variables.
